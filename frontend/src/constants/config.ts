// Feature flags and configuration
export const config = {
  // Enable demo mode buttons (useful for development/testing)
  // Set NEXT_PUBLIC_DEMO_MODE=true in environment to enable
  enableDemoMode: process.env.NEXT_PUBLIC_DEMO_MODE === "true",
};
